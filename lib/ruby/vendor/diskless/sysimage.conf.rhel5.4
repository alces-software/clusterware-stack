#
# For the most part you can skip patching for rhel-like distros, but do:
#       1) make sure tmp and log dirs from your image are pretty clean
#       2) echo "READONLY=yes" > /etc/sysconfig/readonly-root
#       3) mv /etc/rwtab /etc/rwtab.orig or echo > /etc/rwtab
#       4) include this file in /path/to/image/etc/sysimage.conf
#       5) mk-sysimage /path/to/images
#
#       This should work ok for (RHEL, CentOS, Whitebox)5+
#

# distribution
#DISTRO: redhat-el as5.4
#RAMSIZE: 100m

# Writable system areas
#LINKDIR: /var/run -d
#LINKDIR: /var/log -d
#LINKDIR: /var/lib/ntp -d
#LINKDIR: /var/lock/subsys
#LINKDIR: /var/empty/sshd -d
#LINKDIR: /tmp -d
#LINKDIR: /etc/modprobe.d -d
#LINKFILE: /etc/infiniband/openib.conf -d
#LINKFILE: /etc/resolv.conf -d
#LINKFILE: /var/lib/random-seed -d
#LINKFILE: /etc/adjtime -d
#LINKDIR: /var/cache -d
#LINKDIR: /var/lib/nfs -d

DISTRO: redhat-el as5.4
RAMSIZE: 100m

# Writable system areas
LINKDIR: /var/run -d
LINKDIR: /var/log
LINKDIR: /var/lib/ntp -d
LINKDIR: /var/lock/subsys
LINKDIR: /var/empty/sshd -d
LINKDIR: /tmp
LINKDIR: /etc/modprobe.d -d
LINKFILE: /etc/infiniband/openib.conf -d
LINKFILE: /etc/resolv.conf -d
LINKFILE: /var/lib/random-seed -d
LINKDIR: /var/yp/binding -d

